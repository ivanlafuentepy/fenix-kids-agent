# Secretos — NUNCA subir a GitHub
.env
firmware/**/config.h

# Credenciales Google (contienen claves privadas)
config/google_credentials*.json

# Base de datos local
*.db
*.sqlite
*.sqlite3

# Output de scripts (datos de leads)
scripts/output/

# Backups de Airtable (datos sensibles: teléfonos, CI, pagos)
backups/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Knowledge (archivos privados del negocio)
knowledge/*
!knowledge/.gitkeep

# Session state
config/session.yaml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Marketing (imágenes y videos pesados — no van al repo)
marketing/
# Excepto el logo (lo necesita agent/qr.py en producción)
!marketing/logos/
!marketing/logos/LOGO FENIX TRANSPARENTE OFICIAL.png

# Claude Code — solo excluir lo local/sensible
.claude/settings.local.json
.claude/handoff.md
.claude/handoffs/
.claude/plans/
CLAUDE.local.md
logs/

# Contactos personales — NUNCA subir
contactos/

# Cache de wrangler (deploy Cloudflare Pages)
.wrangler/

# Otros asistentes de IA — en este repo solo reina Claude Code
AGENTS.md
.agents/
.codex/

# Media del juego (62 MB: 187 voces de George, guardianes, videos).
# No va a git — se sirve desde Cloudflare Pages (mundo-fenix.pages.dev/assets/)
# y el respaldo vive en R2. Git guardaría cada versión entera de cada binario.
mundo-fenix/assets/
